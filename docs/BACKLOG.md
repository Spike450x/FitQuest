# FitQuest — Technical Backlog

Known gaps, deferred work, and non-blocking improvements. Items graduate out of here when they ship (move to the Shipped section of CLAUDE.md and CHANGELOG.md) or are explicitly cancelled.

This is separate from the **feature backlog** in CLAUDE.md, which covers game content (Achievements page, Reputation, Champions, etc.). This file tracks engineering and polish debt.

---

## Deferred — dependency / risk not yet warranted

| Item                             | Detail                                                                                                                                                                                                                                                                                                                                                                                                                | Trigger to revisit                                                                                                 |
| -------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------ |
| `uuid` audit vulns (×7)          | Inside `firebase-tools` transitive deps. `firebase-tools@15.19.0` is now available (up from 13.35.1) — test on a dedicated branch (`npm install -D firebase-tools@latest`) as the CLI interface may have breaking changes. If the upgrade fixes the uuid chain, close this item.                                                                                                                                      | Run `npm install -D firebase-tools@latest` on a branch and verify `firebase deploy` + rules + functions still work |
| IndexedDB quota on mobile        | Browser can evict the Firestore cache below ~50 MB on low-storage devices. Currently single-user; harmless until mobile audience grows.                                                                                                                                                                                                                                                                               | User base grows to mobile-first                                                                                    |
| `resource-exhausted` jitter      | Retries use fixed delays (`[1_000, 3_000]`). Exponential back-off with jitter would reduce thundering-herd under concurrent load.                                                                                                                                                                                                                                                                                     | Concurrent user count makes fixed delays observable                                                                |
| Calendar 500-log horizon         | `/calendar` reads the shared `statsStore` 500-log cache (no Firestore index), so months older than the most-recent-500 logs render empty — identical to the existing `/stats` "all time" limitation. Fixing needs a date-windowed query + a new `(uid, loggedAt range)` composite index. Not warranted at current scale.                                                                                              | A user reports a blank old month, or the audience skews to daily-heavy loggers (~5+/day for 3+ months)             |
| `avatarId` client-mirrored write | `Character.avatarId` is an optimistic client write (rule only bounds it to `string ≤ 64`) — same trust model as quest-claim / daily-login / reputation. Cosmetic-only today, so worst-case tamper is wearing an "unearned" crest. Needs a Cloud Function re-check **only if** avatars become unlockable rewards or leaderboards ship. Bundle with the other client-mirrored writes flagged for the same CF hardening. | Avatars gate on achievements/rewards, **or** leaderboards arrive                                                   |
| iOS app (Capacitor)              | Native iOS conversion via Capacitor + Next.js static export, for App Store presence, native feel, and Apple Health (HealthKit) auto-logging. Full assessment + phased roadmap in [`IOS-CONVERSION.md`](IOS-CONVERSION.md). Architecture is ready; Phase 1 (static export) is pure web work and needs no Mac.                                                                                                          | Resolve build toolchain (no Mac) — cloud build (Codemagic) or a Mac; then start Phase 1                            |

---

## Polish / nice-to-have

| Item                             | Detail                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Trigger to revisit                                          |
| -------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------- |
| Extract a shared `CombatSurface` | The combat surface (CombatArena + CombatActionBar + the three roll overlays + log) is now composed inline by **three** pages: the arena (`combat/page.tsx`), the dungeon run (`combat/dungeons/run/page.tsx`), and the Wanted Board hunt (`wanted/hunt/[bountyId]/page.tsx`). With three concrete call-sites to factor from, a shared `<CombatSurface encounter={…}>` component is now worth extracting. Deferred from Wanted Board PR2 to avoid refactoring two shipped pages mid-feature. | Now have 3 consumers — extract when next touching combat UI |
| De-dup `getMonsterById`          | `monsters.ts` now exports a map-backed `getMonsterById`; the dungeon run page still has a private `.find`-based copy. Replace the local one with the shared import.                                                                                                                                                                                                                                                                                                                         | Next time `combat/dungeons/run/page.tsx` is edited          |
