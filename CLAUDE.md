# FitQuest — Fitness RPG Development Guide

## Project Overview

A gamified fitness web app built as a full Fitness × Fantasy RPG hybrid. Players log real-world workouts to earn XP, level up a character, unlock gear, complete quests, and battle enemies.

**Stack:** Next.js 15 (App Router) · React 18 · TypeScript 5 · Tailwind CSS · Firebase (Firestore + Auth) · Zustand · Recharts

**Firebase project:** `fitness-rpg-claude`

**Key paths:**

- `src/app/(game)/` — all game screens (dashboard, activities, quests, combat, character, shop, stats, inventory, profile, collections)
- `src/app/(auth)/` — login / register
- `src/components/` — shared UI components
- `src/lib/` — Firebase init + game logic utilities
- `src/store/` — Zustand stores (character, inventory, quests, stats, dungeon, combat, activity, navPreference)
- `src/types/` — TypeScript definitions
- `src/hooks/` — custom React hooks
- `src/middleware.ts` — route protection
- `docs/` — engineering references: [ARCHITECTURE](docs/ARCHITECTURE.md), [FIRESTORE](docs/FIRESTORE.md), [CI](docs/CI.md), [DEPLOYMENT](docs/DEPLOYMENT.md), [GAME-LOGIC](docs/GAME-LOGIC.md), [SECURITY-SETUP](docs/SECURITY-SETUP.md), [SMOKE-TEST](docs/SMOKE-TEST.md), [CHANGELOG](docs/CHANGELOG.md), [UI-UX-MODERNIZATION](docs/UI-UX-MODERNIZATION.md), [ART-ASSETS](docs/ART-ASSETS.md), [HEALTH-INTEGRATION](docs/HEALTH-INTEGRATION.md), [BACKLOG](docs/BACKLOG.md), [BUGS-ENHANCEMENTS](docs/BUGS-ENHANCEMENTS.md), [IOS-CONVERSION](docs/IOS-CONVERSION.md)

---

## Current Status (last updated 2026-05-31 — Combat & class balance pass + damage types)

**Shipped:** MVP phases 1–5 + Spells (21-spell catalog, dice resolution) + Streaks/PRs (Blessing tiers, 1.5× PR XP) + Subclasses (6, level-10 unlock) + Profile analytics + R1 streak boost transparency in victory modal + R4-StageB `logActivity` Cloud Function (server-side daily-cap enforcement, mastery writes) + R4-StageC restore migration (HP/Stamina/Magic capped at formula-derived max server-side) + CI Firestore rules auto-deploy. Code-audit sweeps to 10/10 — full architecture contract (lib wrappers for Firestore/Auth/Cloud Functions via `src/lib/{auth,functions,fetchPlayerData,characterData,activityData,questData,inventoryData,combatData,errors}.ts`), Firestore field normalizers (safe defaults for post-MVP schema fields), store-first reads on stats page, parallel equipment writes in `awardLoot`, combat `useMemo` hoisting, `useCharacter` 30 s TTL with `force` bypass, stat-alloc two-click confirmation, auth form a11y (`id`/`htmlFor`/`autoComplete`), `rotationExpiresAt()`, `(uid, loggedAt DESC)` Firestore index deployed, `rewardedXp`/`rewardedGold` stamped at quest claim, `legendaryDryStreak` Firestore rule validation, Cloud Functions vitest suite, Playwright E2E smoke suite (14 tests), CI updated with functions tests + E2E steps + combined `firestore:rules,indexes` auto-deploy, stats page analytics dashboard (stacked XP chart, Battles Won card, error state, 1000-log limit). **Dungeons** — 4-tier multi-room runs (Goblin Caves/Spider Lair/Dark Sanctum/Dragon's Keep), seeded weekly layouts, stat-check/rest/boss rooms, enrage mechanics, venom DoT, 12 exclusive items, legendary lockout, `claimDungeonRun` CF for atomic rewards. **Achievements** — 6 dungeon badges (Initiate, 4 tier clears, Legendary Haul), gold rewards awarded atomically inside `claimDungeonRun` CF transaction (no race window), parity-tested CF copy, achievement gold breakdown in victory screen, profile badge gallery. **UI/UX modernization** — all 24 items from the original ui-critic audit landed (display font, dark mode, custom heraldic art system, PWA install, sound, design tokens, combat scene redesign, illustrated backgrounds). **Dark mode audit pass** — `InputField` canonical input component (`dark:bg-slate-950`, `sm`/`md`/`lg` variants) introduced; all 14 raw `<input>` elements migrated; comprehensive `dark:` patch across 16 components and pages (stat alloc modal, spell cards, combat dice, shop/inventory/quest/stats tinted surfaces); Playwright E2E dark-mode guard added to `tests/e2e/dark-mode.test.ts`. **Balance backlog cleared** — P0/P1/P2 all shipped, including P0-3 `claimCombatVictory` Cloud Function (server-authoritative daily combat XP cap: 1.0× wins 1–9 → 0.5× wins 10–19 → 0.25× wins 20–29 → 0.1× wins 30+; gold never diminished; parity-tested client/server multiplier; combat page shows live "Daily combat XP" badge). **Combat bug fixes (2026-05-22)** — per-step claim isolation in `handleClaimRewards` (B1: modal closes on CF success, no double-award window, loot-only failures get warning toast); spell action button visible in light mode at disabled state (B2: explicit slate disabled colours replace opacity-40); dark mode in `BattleResultsModal` and in-battle victory banner (B3: white gradients patched, "Drop Only" badge, `BattleLogEntry` border). **Premium spell cards (2026-05-22)** — `PremiumSpellCard` wrapper adds rarity-depth box-shadow, hover lift, and mouse-tracking shimmer (mix-blend-mode: screen); zero re-renders on mousemove via DOM ref mutation; `willChange` promoted only on hover; all 4 SpellCard callsites swapped. **V4 item silhouettes + per-item unique art (2026-05-23)** — `EntityArt category="item"` portraits added to gear/consumable cards in shop and inventory. 45 unique SVG silhouettes authored (18 weapons, 13 armor, 14 accessories) keyed by `item.id`; weapons/armor use `'shield'` frame, accessories use `'medallion'`; no emoji fallbacks. **Reliability & polish sprint** — `lib/retry.ts` (`fetchWithRetry<T>`, `isRetryable`, `STORE_RETRY_DELAYS = [1_000, 3_000]`) wired into all 5 store fetch paths; stale cap meter (spinner + 50% opacity while re-fetching, clears on error instead of deleting entry); double-fetch fix in `ActivityLogForm` submit handler (stale-mark pattern instead of delete + manual refresh); `utcDayStartMs(date?)` helper extracted to `streaks.ts` with injectable date param on `fetchTodayLogsForType`; Firestore IndexedDB offline persistence (`persistentLocalCache` + `persistentMultipleTabManager`) with SSR + hot-reload guards; sign-out store flush for all 5 stores; `npm audit fix` (brace-expansion + qs vulns patched). 3D tumbling dice + phased roll sounds (V1+V2) (2026-05-22). **CSP + Die3D + emulator sprint (2026-05-23)** — CSP `connect-src` was missing `https://*.cloudfunctions.net`, blocking all Cloud Function calls (`claimCombatVictory`, `logActivity`) at the browser level; fixed alongside `worker-src blob:` for canvas-confetti. `Die3D` extended with `format='number'`, `size='xl'`, and `color` prop; `ActionRollOverlay` migrated from removed `D10Face` — all combat dice now use the 3D cube. `firebase.ts` emulator support completed with `connectFunctionsEmulator` (port 5001). **Stability-to-A sprint (2026-05-23)** — `OfflineBanner` copy updated to accurately describe Firestore's queued-write replay behaviour on reconnect. New `tests/e2e/global-setup.ts` seeds an Auth + Firestore emulator test user/character via REST API and saves Playwright `storageState`; new `tests/e2e/authenticated.test.ts` smoke-tests 9 game routes via the saved storage state; `playwright.config.ts` gained a second `authenticated` project that applies the storage state; CI now starts the auth+firestore emulators before E2E and passes `NEXT_PUBLIC_USE_FIREBASE_EMULATOR=true`. `scripts/audit-check.mjs` replaces the two `continue-on-error` `npm audit` steps — fails the build on any high/critical vulnerability, logs moderates as warnings (firebase-tools transitive uuid<9 chain documented in `docs/SECURITY-SETUP.md § Known devDependency vulnerabilities`). `@next/bundle-analyzer` wired up behind `npm run analyze`. Item silhouettes (55 SVG functions + `ITEM_SILHOUETTES` map, ~1030 lines) moved out of `silhouettes.tsx` into new `src/components/art/item-silhouettes.tsx`, so non-item routes (combat/character/dashboard) no longer pay the cost in their shared chunk. **Dungeon ↔ arena combat parity (2026-05-24)** — dungeon combat rooms now expose the full arena action set (Attack, Magic, Roll Ability 6d6, Cast Spell, Rest, Meditate, Use Item, Flee) with full passive / lifesteal / crit / execute / momentum / per-round-restore integration on every action. Previously dungeons were attack-and-flee only and even basic attacks bypassed `applyOutgoingPassives` and `resolveLifesteal`, leaving subclass builds inert inside dungeons. Refactor extracts a shared combat layer used by both pages: new `src/components/combat/` surface (overlays + action bar + log + portraits + types), new `src/lib/gameLogic/combatActions.ts` pure resolvers, new `src/hooks/useCombatEncounter.ts` hook. Arena page shrank ~1,000 lines. Dungeon page mounts the hook for combat/boss phases and keeps stat-check / rest / transition / boss-victory / defeat phases as today. Dungeon-only mechanics (Venom DoT, Necro Shield, Dragon ignore-DEF, Broodmother +ATK, boss-room Flee disable) inject via a new `CombatModifiers` seam — arena passes `undefined`, every hook short-circuits to identity (zero behaviour change for arena). `useCombatEncounter` is store-agnostic: `onResourceChange` is a local-mirror-only signal, Firestore writes happen exactly once per encounter inside `onVictory` / `onDefeat` / `onFlee`. Arena keeps `claimCombatVictoryCF`, dungeon keeps `advanceRoom` per cleared room + `claimDungeonRunCF` at boss-victory claim. 15 new vitest specs cover each resolver + every modifier hook slot (arena-equivalence test confirms `modifiers: undefined` produces identical state deltas to the legacy inline handlers). 666 total tests pass; suite still under 15 s. **Test coverage expansion (2026-05-23)** — 24 new vitest files (215 new tests, 438 → 653; suite still under 12 s). Coverage: `characterStore` (41 tests — fetch TTL, level-up bonuses, stat-allocation caps, subclass gating at level 10, monster pity reset/increment, mastery local-apply, restore local-apply, name updates), `dungeonStore` (19 — legacy claimed-run cleanup, startRun gating on HP/gold/daily limit, advanceRoom accumulation, completeRun CF-finalized fast-path), `statsStore` (10 — TTL cache, retry flag, error capture). `useCharacter`, `useOnlineStatus`, `useCombatBursts`, `useRecentActivity`. `characterData.normalizeCharacter` (required-field validation + post-MVP defaults), `questData.normalizeActiveQuest`, `inventoryData.normalizeInventoryItem`, `combatData`, `dungeonData` (all 7 exports), `auth` (signIn / signUp / logOut / verifyBeforeUpdateEmail / updatePassword / updateProfile), `functions.claimDungeonRunCF` idempotent-no-op for `functions/already-exists`, `errors.captureError`. Components: `InputField`, `Button`, `XPBar`, `EmptyState`, `ErrorBanner`, `OfflineBanner`, `Heading`, `Card`, `Modal` (a11y attrs, Escape closes, body-scroll lock). `vitest.config.ts` `coverage.include` broadened to `src/lib/**`, `src/store/**`, `src/hooks/**`, `src/components/**/*.{ts,tsx}`; thresholds remain gated only on `src/lib/gameLogic/**` (80/80/70/80). `@vitejs/plugin-react` added as a devDep so vitest 4 / rolldown can parse JSX in `.tsx` test files.

**Deferred items (not blocking, revisit when applicable):**

- `uuid` vulns (7 moderate) inside `firebase-tools` transitive deps — `npm audit fix --force` would install `firebase-tools@1.2.0` (breaking); watch Firebase CLI release notes and run `npm update firebase-tools` on a branch when a Node-24-compatible fix lands. CI now logs these as warnings via `scripts/audit-check.mjs` rather than blocking the build (high/critical still block). Documented in `docs/SECURITY-SETUP.md § Known devDependency vulnerabilities`
- IndexedDB quota on mobile — browser can evict the cache below ~50 MB on low-storage devices; monitor if user base grows to mobile-first audience
- `resource-exhausted` jitter — retries currently use fixed delays; exponential back-off with jitter would reduce thundering-herd if the app ever has concurrent users; not warranted at current scale

**Spell-card front/back flip (2026-05-24)** — `PremiumSpellCard` becomes an MTG-style flippable card. New `SpellCardBack` component renders a uniform "FitQuest Spellbook" reverse face: rarity-tinted border + header band + footer plate matching the front silhouette, with a central hexagonal `HeraldicFrame` sigil (sword-and-spark glyph) ringed by five colored magic-school orbs (heal/defense/stun/damage/lifesteal) in an MTG-pentagon layout. Click the card body to flip; flip and the existing hover-tilt compose on one inner element via `transform-style: preserve-3d` + `backface-visibility: hidden`. `role="button"`, `aria-pressed`, Enter/Space keyboard flip, and `useReducedMotion` from framer-motion gates the 600 ms cubic-bezier transition. The action button (Buy/Equip/Cast) stops click propagation so it never triggers a flip. All 4 callsites (shop, inventory loadout × 2, combat selector) inherit the flip with zero API change. 674 total tests pass (+8 specs in `PremiumSpellCard.test.tsx`).

**Bugs & enhancements backlog (2026-05-25)** — `docs/BUGS-ENHANCEMENTS.md` cataloged 9 bugs and 8 enhancements. All 9 bugs and 7 of 8 Phase 4 enhancements are now shipped (E5/E9 deferred to Phase 5 pending design decisions). 690 total vitest tests pass. Phase 4 shipped: **E1** stat-check flavor text per dungeon tier; **E2** emoji→SVG icon migration (stat-icons.tsx + action-icons.tsx, StatBar accepts ReactNode, activityIcons.ts gains getActivityIconSvg); **E3** spell card shimmer adaptive in light/dark mode; **E4** weekly spell rotation in shop (5 featured, getWeeklySpells + shopRotation.ts); **E6** out-of-combat consumable "Use" button in inventory (blocked during dungeon runs); **E7** spell school sounds (8 new synths) + fullscreen school-colored flash on result reveal; **E8** ability damage formula breakdown in DiceRollOverlay; **E10** mobile responsiveness sweep across all game routes. Bugs shipped: **B1** quest reroll deleteField fix; **B2/B3** Cloud Function investigation (pending log access); **B4** refreshPlayerState() helper + shop buyItem authoritative refetch; **B5/B6** gear equip raises max only (DQ1/DQ2); **B7** quest page tab switcher; **B8** SpellRollOverlay monster counter-attack panel; **B9** combat nav lock + beforeunload guard (combatStore + CombatSafeLink).

**Content-scaling PR1 (2026-05-26)** — Foundation slice of the 5-PR ~2× content scaling drop (per `plans/i-want-to-before-ethereal-waffle.md`). Adds a **7th stat: Spirit** (primary, cap 50, joins STR/WIS/AGI) — class starting values 3/7/3 (Warrior/Wizard/Rogue), multipliers 0.9×/1.2×/1.1×; spell+ability crit math (`spellCritChance` +1%/pt cap 40%, `spellCritDamage` +0.5%/pt cap +25%, `rollSpellCrit` helper in `combat.ts` with parity hook in `functions/src/gameLogic/combat.ts`); integrated into `resolveAbilityAction`/`resolveSpellAction` in `combatActions.ts` — fires only on a successful (non-fizzle / requirement-met) action that deals damage; new `spiritCrit` and `spiritCritMultiplier` fields on `RoundEntry`. Legacy character docs backfilled on next fetch via the existing agility-migration path in `characterStore`. Adds a **7th activity: Meditation** (60-min daily cap, 0.2 magic-per-minute restore, Spirit mastery on the 5/15/25 milestone curve) — first activity to live in both `MASTERY_ACTIVITIES` and `RESTORE_ACTIVITIES`; lotus-pose SVG in `action-icons.tsx`; new tab in `ActivityLogForm`; chart color `#a78bfa` in the stats page legend. `StatAllocModal` extended to 5 stat options with a Spirit pick. **Cloud Function parity**: `validTypes`, `RESTORE_MAP`, `MASTERY_CONFIG`, `MASTERY_ACTIVITIES`, `RESTORE_ACTIVITIES`, `statCap` (spirit primary), `RESTORE.MAGIC_PER_MEDITATION_MINUTE` all extended in `functions/src/`. **Quests**: 5 daily + 2 weekly meditation quests + 1 daily combo (`mind-body`) + 1 weekly combo (`mindful-mover`) → DAILY_QUEST_POOL grows 28→34, WEEKLY_QUEST_POOL 14→17. 24 new vitest specs (720 → 744): full crit-math coverage, end-to-end meditation surface coverage, parity tests still green.

**Content-scaling PR2 (2026-05-26)** — Monster roster + new mechanics slice. **10 new arena monsters** (Mud Imp / Boar Runt / Bog Lurker / Iron Husk / Frost Wraith / Gloom Knight thicken L1–8 variety; Obsidian Golem / Ashwyrm / Void Revenant / Storm Djinn fill the L11–14 progression gap between Ancient Dragon and Dragon King). **3 new monster mechanics**: `siphon` passive (drains player stamina per landed hit — applied post-damage in all three combat resolvers, surfaces as `monsterSiphon` in the round log), `armor-pierce` passive (flat reduction to player effective DEF — new helpers `monsterArmorPierce` + `effectivePlayerDefenseVsMonster` in `combat.ts`, wired through `calculateRound` / `rollRunAway` / `spells.ts` counter / `abilities.ts:rollMonsterAttack`, log field `monsterArmorPierce`), and `summon-add` active (one-time HP bump to current + cap, tracked via new `FightState.monsterBonusHp`, regen/vampiric caps now use `effectiveMonsterMaxHp(state)`, log field `monsterSummonAddHp`). Dungeon `dark-sanctum` pool extended with `iron-husk` + `gloom-knight`; `dragons-keep` extended with all four L11–14 monsters. **Bestiary backend**: new `Character.monstersKilled?: Record<string, { killCount; firstKilledAt }>` field, defaulted by `normalizeCharacter`; `updateMonsterPity` now writes both `legendaryDryStreak` and `monstersKilled` in the same Firestore call. **Art**: 10 new monster SVGs registered in `MONSTER_SILHOUETTES`. 16 new vitest specs (744 → 760): armor-pierce math, `calculateRound` integration, siphon value reads, catalog + dungeon-pool registration assertions. Suite under 13 s.

**Content-scaling PR3 (2026-05-26)** — Items + art slice. **56 new items** (22 weapons / 12 armor / 16 accessories / 6 consumables) roughly doubling the catalog; new tier-appropriate gear exists at every rarity from common through legendary, fills out the L11–14 loot space the new monsters drop into, and repairs the previously-flagged Agility (≥6 new AGI items) and Spirit (≥4 new Spirit items) gear gaps so both stats now have full build paths. Includes **9 new legendaries** (8 gear + Phoenix Draught consumable): World-Ender / Cosmic Codex / Shadowblade Zenith / Crown of Mind (weapons), Guardian Bulwark / Starfire Vestments (armor), Eye of Eternity / Twin Suns Pendant (accessories), Phoenix Draught (legendary HP consumable). **New `ConsumableEffect` variant**: `{ type: 'multi'; restores: Array<{resource; amount}> }` for combo elixirs (Battle Stim, Spirit Tea, Sage's Brew). `ConsumableEffect` is now a discriminated union; `inventoryStore.useConsumable` applies multi-step restores in one pass. Shared formatting helpers (`describeConsumableEffect`, `consumableEffectColorClass`, `consumableEffectColorHex`) in `items.ts` back both the inventory page and the in-combat action bar so future effect variants surface uniformly. **Functions parity**: `LEGENDARY_ITEM_IDS` extended with all 9 new legendaries; `GEAR_STAT_BONUSES` adds the 4 new items with stamina/health bonuses (cloth-shirt, tortoise-charm, guardian-bulwark, twin-suns-pendant) so server-side resource-max math stays accurate. **L11–14 loot tables**: Obsidian Golem / Ashwyrm / Void Revenant / Storm Djinn now drop the new tier-appropriate legendaries and epics instead of pointing at older tier-4 fillers. **Art**: 56 new SVG silhouettes in `src/components/art/item-silhouettes.tsx` (one per item ID); reuses the existing `currentColor` + Tailwind-class vocabulary; non-item routes stay lean via the existing lazy-loaded art module. 20 new vitest specs (760 → 780): catalog registration per item type, legendary parity, silhouette coverage for every new id, AGI / Spirit gear-coverage assertions, multi-effect shape spot-checks, helper output coverage for all four resource colors and label variants. Suite still under 14 s.

**Content-scaling PR4 (2026-05-29)** — Spells + new spell effect slice. **14 new spells** grow the catalog 21→35: 6 generic (Cinder Spark, Soothing Light, Emberstorm, Radiant Bulwark, Cataclysm, Divine Sanctuary), 2 Warrior (Rending Cleave, Seismic Slam), 2 Wizard (Incinerate, Glacial Prison), 1 Rogue (Rupture), and **3 legendary class spells** (Worldbreaker / Stellar Collapse / Thousand Cuts, loot-only). **New `SpellEffect.dotDamage` field** (`{ perRound, rounds }`) — bleed/burn applied to the monster on a successful cast, ticking at the start of each subsequent offensive round (bypassing defense) via a new `FightState.monsterDots` array keyed by spell id (re-cast refreshes; different spells + dungeon venom stack independently). Ticked in `runPreAction` after regen so it works in **arena and dungeon** with zero modifier plumbing; a DoT-kill ends the fight as a win with no counter-attack. New `RoundEntry.monsterDotDamage` + a "suffers N bleed damage" modifier-note surface it in the log. `spellEffectKey` routes any `dotDamage` spell to the `fire` school (crackling sound + orange flash + flame silhouette), with stun/lifesteal still taking precedence. **`cleanseSelf` deferred** (no monster applies player DoT yet — would be dead code). Legendaries are loot-only AND obtainable: drop from the Dragon King boss (8% each) + matching L11–14 arena monster (3% each); the 11 buyable spells grow the weekly shop rotation. Stellar Collapse uses a 5d6 three-of-a-kind (`SpellRollOverlay` dice row already wraps). **Functions parity**: `LEGENDARY_ITEM_IDS` + 3 spell ids (no server-side spell resolution exists, so `dotDamage` needs no other parity copy). 17 new vitest specs (780 → 797): DoT timing / ticking / expiry / refresh-vs-stack / combined-tick / DoT-kill-win / fizzle-no-DoT, `spellEffectKey` fire routing, catalog + buyable/loot-only/legendary-parity assertions.

**Content-scaling PR5a (2026-05-29)** — Bestiary + Collection surfaces + quest expansion. **New `/stats/bestiary` route**: card grid of all `MONSTER_CATALOG` monsters (level-sorted) + 4 dungeon bosses; slain monsters show portrait + kill count + first-killed date from `character.monstersKilled` (written since PR2), undiscovered show greyed silhouette + "???". Bosses aren't in `monstersKilled` (pruned to catalog ids) so boss-defeated derives from the tier-clear achievement. **New `/stats/collection` route**: owned-vs-total item grid grouped by type, sorted common→legendary, overall completion % bar; spells render their effect-school silhouette via `spellEffectKey`. **New `StatsTabs`** Link-segmented control (Overview / Bestiary / Collection) above all three. **New `src/lib/gameLogic/collections.ts`** exports `bestiaryProgress` / `collectionProgress` / `BOSS_TIER_ACHIEVEMENT` / `tierName` so completion math is unit-tested. **Quests**: DAILY_QUEST_POOL 34→61 (+27, incl. 5 Spirit-building meditation combos), WEEKLY_QUEST_POOL 17→31 (+14). **OG epic spells now obtainable**: Titan's Fury / Void Collapse / Phantom Assault (previously orphaned loot-only) now drop from Dragon King / Necromancer / Broodmother bosses. 14 new vitest specs (797 → 811).

**Content-scaling PR5b (2026-05-30)** — 24 new achievements grow the catalog 6→30 across combat / activity / mastery / quest / collection categories. **Server-authoritative for 17 IDs** — `claimCombatVictory` CF accepts a `flawless` input, increments `totalCombatWins` + per-monster `monstersKilled[id].killCount` inside its existing transaction, runs `checkNewCombatAchievements`, and merges new IDs + their gold reward atomically (`first-blood`, `centurion`, four `slayer-*` for L11–14 monsters, `untouched`). `logActivity` CF folds achievement evaluation into its mastery transaction, now also tracking lifetime `activityLogCounts[type]` per activity (distinct from the mastery counter); for water logs it pre-queries the past 7 days of water docs to compute `waterStreakDays` before the txn. Drives `iron-body`/`marathoner`/`well-fed`/`well-rested`/`enlightened`/`hydration-streak` + mastery tiers `apprentice`/`journeyman`/`master`/`polymath`. **Client-authoritative for 7 IDs** — `questStore.claimReward` writes quest achievements (`quest-novice`/`-veteran`/`-legend`/`weekly-perfectionist`) tracked via new `totalQuestsClaimed` + `weeklyQuestsClaimed` fields; new `useCollectionAchievementSync` hook (mounted in `(game)/layout.tsx`) computes collection achievements (`bestiary-complete`/`legendary-hoarder`/`armory`) from inventory + bestiary on every change. These are optimistic client writes — the existing CF transactions do NOT re-check them. Worst-case tamper is a few hundred gold per fabricated unlock; harden via a CF re-check when leaderboards arrive. **Parity** — `functions/src/gameLogic/achievements.ts` mirrors every gold value + threshold + checker; `achievements-parity.test.ts` grew from 11 → 19 assertions covering every new constant on equivalent fixtures. **Tests**: 35 new vitest specs (811 → 846). Achievement-unlock toasts surface on combat-victory claim, activity log result, and quest claim. Existing profile badge gallery renders all 30 IDs from `ACHIEVEMENTS` with no per-id wiring needed.

**Health-data integration scaffold (2026-05-30, Strava + Garmin, free)** — Foundation for auto-logging real runs/workouts from a connected app. Two providers share one provider-neutral ingestion core, feature-flagged off behind `NEXT_PUBLIC_HEALTH_SYNC_ENABLED`; full design + runbook in [docs/HEALTH-INTEGRATION.md](docs/HEALTH-INTEGRATION.md). **Strava is the works-today path** (free, self-serve, no approval; also pulls Garmin/Apple Watch/Fitbit workouts the user syncs to Strava) via `createStravaAuthUrl`/`stravaOAuthCallback` (OAuth2) + `stravaWebhook` (subscription handshake + notification → `GET /activities/{id}` → map; 6-hour tokens auto-refreshed by `stravaApi`); no steps/sleep. **Garmin-direct is coded but gated on enterprise Developer Program approval** (adds steps + sleep) — `createGarminAuthUrl` (OAuth 2.0 PKCE) + `garminOAuthCallback` + `garminWebhook` (Push), inert until its secrets are set. **Shared write core** — extracted `logActivityCore` (`functions/src/logActivityCore.ts`) from the `logActivity` callable so device-synced and manual logs run the _identical_ authoritative sequence (server-side daily cap → log write → mastery milestone → resource restore → achievements); the callable is now a thin auth+validation wrapper (no behaviour change, existing parity tests guard it). **New Cloud Functions** (repo's first `onRequest` HTTP fns + first Functions **secrets**) — `createGarminAuthUrl` (onCall, OAuth 2.0 PKCE start), `garminOAuthCallback` (onRequest, code→token exchange), `garminWebhook` (onRequest, Garmin "Push" ingestion via the shared core). **OAuth tokens held server-side** — new server-only `healthTokens` + `healthOAuthStates` collections (rules deny ALL client access); client-readable `healthConnections` holds status only, no tokens. **Pure, tested modules** — `gameLogic/garminMapping.ts` (Garmin `activities`/`dailies`/`sleeps` → run/workout/steps/sleep, clamped to `ACTIVITY_AMOUNT_MAX`), `gameLogic/healthDedupe.ts` (provider-namespaced event-id idempotency + cumulative daily-step delta math via a `healthDailySnapshots` cursor), `garminOAuth.ts` (PKCE). New optional `ActivityLog.source` (`'garmin'`) drives a "⌚ synced" badge in the dashboard feed. **Client surface** — `lib/health.ts`, `lib/healthData.ts`, `useHealthConnections`, `/profile/connections` page (feature-flagged), profile link. Core is provider-neutral so a paid aggregator (Terra/Rook) or a second provider can be added later as a thin adapter. **CSP unchanged** (connect flow is a full-page nav to Garmin; webhook/callback are inbound). **Apple Health intentionally out of scope** — HealthKit has no web API; needs a native iOS/Capacitor shell. A few Garmin endpoint/payload constants are flagged "verify post-approval" in the code + doc §8. 28 new functions vitest specs (functions suite 16 → 44); 884 root tests still green.

**Balance pass (2026-05-30)** — Post-content-scaling tuning, addresses all 5 issues + 3 polish gaps surfaced by `/balance-check`. **Combat XP daily cap tightened** — `combatXpDailyMultiplier` 1.0× tier moved 0–9 → 0–4 wins; 0.5× / 0.25× / 0.1× breaks shift to 5–14 / 15–24 / 25+. Daily combat-farm ceiling drops from ~7 500 → ~3 500 XP, closer to activity-logging ceiling. Server parity test + combat-page badge title copy updated in lockstep. **Quest reroll cost scales with level** — `QUEST_REROLL_COST = 100` (flat) → `questRerollCost(level) = 100 * max(1, floor(level/5))`. L1–9 = 100 g, L10–14 = 200 g, L15+ = 300 g. **`MAX_STREAK_SHIELDS` 1 → 3** with dashboard count display. **3 shop-buyable legendaries**: Merchant's Codex (4 000 g, +15 WIS/+5 STA), Champion's Sigil (3 500 g, +5 each STR/WIS/AGI/SPR), Gilded Bulwark (5 000 g, +18 DEF/+8 HP). All `lootOnly: false`, stat budgets deliberately under loot-only legendaries (alternatives not upgrades). Functions-side `LEGENDARY_ITEM_IDS` + `GEAR_STAT_BONUSES` extended in parity. **Welcome-back ephemeral session boost** — returning players (absent ≥ 14 days with no active streak tier) get +30 % loot + +10 % XP for the session via a dismissible banner. Pure derivation from `streakData.lastLogDate`, no schema change. **Polymath progress widget on profile** — 4 pip-rows showing mastery counts vs the 5-each threshold; turns emerald-✓ on full completion. **`arcane-archive` achievement (31st)** — own every spell in the catalog. 800 g reward, client-mirrored via `useCollectionAchievementSync`. **Daily login bonus** — first dashboard mount each UTC day grants `min(75, 25 + 5 × level)` gold + `min(150, 10 × level)` XP via toast. Tracked via new `Character.lastLoginGrantedDate`. Client-mirrored optimistic write (matches PR5b pattern). **Tests** — 16 new vitest specs (846 → 862); existing combat-XP fixtures + parity test updated.

**Code-audit fix pass (2026-05-30)** — Closes every finding from the post-sprint audit: 2 correctness bugs + 5 code-quality items + 3 cheap Consider wins. **Must Fix #1** — `useDailyLoginBonus` now stamps `lastLoginGrantedDate` BEFORE awarding XP/gold so a crash between writes can't replay the bonus. **Must Fix #2** — `questStore.claimReward` weekly stale-count leak fixed (`weeklyClaimsThisWeek` only computed inside the `isWeekly` branch). **Unified write helper** — new `characterStore.applyCharacterPatch(patch, opts?)` action owns the read-character + `updateCharacterDoc` + functional `setState` flow that 3 client-mirrored writers (`useCollectionAchievementSync`, `useDailyLoginBonus`, the achievement-merge in `questStore.claimReward`) were duplicating. Functional updater inside is mandatory — eliminates the race where one hook's stale snapshot could clobber another's local-state update. `questStore.claimReward` split via a new `applyQuestAchievementSideEffects` private helper (107 lines → 30). **Code-quality wins**: `useCollectionAchievementSync` short-circuit now covers all 4 collection IDs (was missing `arcane-archive`); `useDailyLoginBonus` narrowed store-subscription slices (uid + level + lastLoginGrantedDate, not full character); `inventoryStore.useConsumable` signature widened from 7 positional params to one `{ inventoryItemId, resources: {…} }` options object (3 callsites updated); `characterData` Spirit + Agility backfill switched from `??` to `'spirit' in rawStats` so a stored zero is never re-overwritten by the class default. **Tests**: 6 new vitest specs (876 → 882) — 5 `applyCharacterPatch` cases (no-op, patch-and-merge, functional-updater-race, skipFirestore, error capture) + 1 zero-preservation regression. Functions parity test grew to 20 assertions with the missing `void-revenant` → `slayer-revenant` fixture.

**Collections page + mobile nav overhaul (2026-05-30)** — PRs #147–151. **`/collections` route** (PR #147/148): dedicated top-level hub with three sub-pages (`/collections`, `/collections/bestiary`, `/collections/collection`) mirroring the stats-page bestiary/collection views; summary strip chips link deep into each sub-page. **Mobile overflow panel** (PR #148): "More" button in the bottom nav opens a framer-motion animated overlay listing non-pinned nav items, replacing the prior drawer; panel is keyboard-accessible, closes on outside click, and is dismissed with a drag handle. **Nav customizer** (PR #148): long-press or Edit button opens a reorderable pinned-tab customizer (max 5 pins via `useNavPreferenceStore`); preferences persisted to localStorage. **PR #150** added: swipe-to-dismiss on the overflow panel (`drag="y"`, 60 px offset or 400 px/s velocity threshold + 8 ms haptic via `navigator.vibrate`); animated onboarding badge on the More button (orange ping, 3 iterations, exits with scale+fade via AnimatePresence when the customizer is first opened — `hasSeenCustomizer` persisted in Zustand); deep-link chips on the collections summary strip converted to `<Link>` with hover transitions; `src/lib/navConfig.ts` extracted as single source of truth for `NAV_ITEMS` / `ALL_NAV_HREFS` so layout and store can't drift. **PR #151** refined the bottom-sheet with a composite box-shadow (inset top-edge glass highlight + subtle upward glow + directional drop shadow, separate dark variant), capped the ping animation at 3 iterations via `[animation-iteration-count:3]` Tailwind arbitrary class, and tightened the badge exit animation timing. 14 navPreferenceStore + navConfig vitest specs added (846 → 860 total).

**Reputation + Wanted Board — PR1 foundation (2026-05-31)** — First slice of the roadmap's foundation economy. **New second currency — Reputation**, dual-track: a `spendableReputation` wallet + a monotonic `lifetimeReputation` tracker that drives a 5-tier rank (Newcomer 0 / Known 500 / Respected 1500 / Renowned 4000 / Legendary 10000). New pure `src/lib/gameLogic/reputation.ts` (`REPUTATION_RANKS`, `reputationRank`, `nextReputationRank`, `reputationProgress`). Rank is a visible badge + progress bar this PR — the features it gates (vendor, NPCs, raids) ship later, so **no rank stat bonuses yet** (deviation from spec, deferred). **New `/wanted` route — the Wanted Board**: daily-rotating fitness bounties (`src/lib/gameLogic/bounties.ts`, `BOUNTY_POOL` + `getBountyDef`) that grant Reputation on completion. New `bountyStore` + `bountyData.ts` + `activeBounties` Firestore collection, all structurally mirroring the quest system (uid-only read + client-side expiry filter — **no index** — 30 s TTL, `_fetching` guard). Bounty progress hooks into the SAME `ActivityLogForm` callsite as quest progress, so one log feeds both reward tracks (**intentional double-dip** — bounties are a parallel earning surface). **Loot claim path only**: `claimBounty(id, { path })` grants Reputation to both wallets via `characterStore.applyCharacterPatch` (client-mirrored, like quest claims / daily-login). The **Fight fork** (combat encounter for a bigger payout) + a hardening `claimBounty` CF are deferred via the typed `path` seam. Surfaces: dashboard Reputation chip beside gold, profile rank card, `Target` nav entry (defaults to overflow nav), bounty store joins the 6→7 sign-out flush. Rules: new `activeBounties` validators (clone of `activeQuests`); character reputation fields bounded + `lifetimeReputation` monotonic and delta-capped (≤2000/write). 38 new vitest specs (884 → 922): rank math, pool registration, `bountyData` normalize, store actions incl. dual-wallet grant + deferred-fight seam.

**Wanted Board Hunt fork — PR2 (2026-05-31)** — Differentiates the Wanted Board from quests by giving it combat. Most bounties are now **Hunts**: logging the activity _tracks down_ a named, level-scaled target (the unlock → `completedAt`), then the player fights it on a new **`/wanted/hunt/[bountyId]`** route; **winning** collects the Reputation (the wired-up `claimBounty(id, { path: 'fight' })` seam). A thin set of activity-only **standing** bounties remains as the rest-day floor; `fetchAndAssignBounties` composes the daily board as 2 hunts + 1 standing. **Combat reuse**: the hunt route mounts the real `useCombatEncounter` and reuses the arena's combat surface (the third consumer after arena + dungeon-run; a shared `CombatSurface` extraction was deliberately deferred — see `docs/BACKLOG.md`). Win → `claimCombatVictory` CF for the fight's own XP/gold (**no item loot** — locked design) + Reputation via the bounty store. **Loss is a soft failure** — no character reset (a deliberate divergence from the arena), the unlock persists for a free retry. Targets are pinned at assignment via the pure `pickHuntMonster(level, band, seed)` (band is RELATIVE to player level → always viable, varied bite); hunts pay more Reputation than any standing bounty (130/170/220 by `combat.levelBand`). New `getMonsterById` map lookup in `monsters.ts`. **Schema/rules**: `BountyDef` gains `kind` + `combat.levelBand`; `ActiveBounty` gains `combatMonsterId` (immutable) + `combatWonAt` (write-once during claim); `activeBounties` validators extended. **Known consequence**: hunt wins route through `claimCombatVictory`, so they count toward the daily combat-XP cap + bestiary/achievements — intended (a hunt is combat); the hunt page surfaces the XP-multiplier warning. 12 new vitest specs (922 → 934) + 5 new rules specs (122 → 127).

**Activity Calendar + Profile/Settings split + preset avatars (2026-05-31)** — **New `/calendar` route**: a read-only month/week calendar of logged activities. Month grid renders per-day activity color-dots (from the now-shared `ACTIVITY_COLORS`, moved out of the stats page into `activityIcons.ts`) with a `+N` overflow chip and a ⌚ marker for `source`-tagged synced logs; the week view lists each day's activities; clicking a day opens a detail `Modal` (label, amount/unit, XP, synced badge, time). Reuses the existing `statsStore` 500-log cache — **no new store, fetch path, or Firestore index**. New pure `src/lib/gameLogic/calendar.ts` (`localDayKey` / `monthMatrix` / `weekDays` / `groupLogsByDay`) buckets by **local** day (deliberately distinct from the UTC `streaks.ts` keys, which back streak math). New `Calendar` nav entry (`navConfig` 10→11 items; overflow by default). **Profile / Settings split**: the old one-page `/profile` "Account Settings" was split — `/profile` is now identity/account (avatar, name, email, password, Reputation card, Polymath, Collections link) and a new `/settings` route holds preferences (theme, sound, nav customizer, a Connections link, install). Shared `SettingsCard` extracted to `src/components/ui/SettingsCard.tsx`; the two pages cross-link. **Preset avatar picker**: new optional `Character.avatarId` chosen from a curated heraldic-crest catalog (`src/lib/gameLogic/avatars.ts` — `AVATAR_OPTIONS` / `getAvatarOption` / `resolveAvatar`) rendered through the existing `EntityArt` — **no image upload, no Firebase Storage**. New `CharacterAvatar` component shows the chosen crest (class-crest fallback) in the profile header + the layout header button (replaces the bare initial). Persisted via the client-mirrored `applyCharacterPatch`; rule: `avatarId` validated as `string ≤ 64`. 18 new vitest specs (922 → 940): calendar date helpers + avatar catalog/resolution.

**Title equip-from-rank-up + combat-banner title (2026-05-31)** — `RankUpCelebration` gains a one-tap "Equip “{title}”" button (with a "Not now" escape) that sets `activeTitle` inline via `applyCharacterPatch`, closing the unlock→wear loop without a character-sheet detour. The equipped title (`resolveActiveTitle`) now also renders under the player name on the **arena and hunt victory banners**. Pure UI; no schema/rules/logic changes; 958 tests green.

**Reputation rank-up celebration + title flair (2026-05-31)** — New `RankUpCelebration` component (mirrors `LevelUpCelebration`, mounted in `(game)/layout.tsx`) pops a violet cinematic modal — Award glyph + confetti + sound — when the player's Reputation rank advances a tier, announcing the newly-unlocked title (baseline-on-first-observation guard avoids false-triggering on load). The equipped title (`resolveActiveTitle`) also shows as an italic flourish under the name on the dashboard hero. No new game-logic exports/schema/rules; 958 tests green.

**Reputation progression surfacing — ranks ladder, titles, tracking (2026-05-31)** — Turns Reputation from a bare number into a visible progression system. Each of the 5 ranks now grants an equippable flavorful **title** (`ReputationRank.title`: Greenhorn / the Named / the Respected / Renowned Hunter / the Legendary). New pure helpers in `reputation.ts` (`unlockedRanks`, `isRankUnlocked`, `resolveActiveTitle` — equipped-if-unlocked-else-current, gracefully ignores a stale/forged equip). New `ReputationLadder` (all 5 tiers: threshold, what each unlocks, locked/unlocked/current marker) + `ReputationTitles` (tap-to-equip locked/unlocked grid → `Character.activeTitle` via `applyCharacterPatch`) in `ReputationChip.tsx`. **Character sheet** (`/character`) gains a Reputation hub card (rank bar + wallet + bounties-completed + equipped title + collapsible ranks/titles) — the main tracking home; Wanted Board adds a collapsible ladder + title; profile shows title + bounties + link. **New monotonic `Character.bountiesCompleted`** — incremented on every bounty claim (loot + hunt) inside the shared `grantBountyReward` writer; collect toasts append a "🏆 Bounty #N" trophy line. **Rules**: `bountiesCompleted` (monotonic, ≤10/write) + `activeTitle` (valid rank id) validators. 6 new vitest specs (952 → 958) + 5 new rules specs.

**Monster special moves + always-visible enemy roll + combat-audit fixes (2026-06-01)** — Gives monsters chance-based **special moves** rolled on their counter-turn _independent of the player's action_ (a `/code-audit` follow-up answered "monsters should be able to roll their own special, not just mirror a normal attack"). New data-driven `MonsterDef.specialMoves` + pure `rollMonsterSpecial(monster, rng?)` (Bernoulli per move in catalog order, **first-fires-wins**, ≤1/counter). Four effects: **heavy** (×N damage), **pierce** (ignores your armor this hit), **burst** (the counter becomes armor-ignoring **magic** regardless of the monster's school), **drain** (monster heals from the hit). Threaded through an extended `incomingMonsterDamage(…, special?)` + new `monsterSpecialDrainHeal` / `effectiveAttackType` helpers, returned alongside `monsterRoll` from `calculateRound` / `rollMonsterAttack` (abilities) / `resolveSpell`, and folded into the same HP cap as the `vampiric` passive in all three resolvers. **Drain** stacks with vampiric; specials are suppressed on stun/dodge/kill. Assigned to 13 arena monsters (L5+ only — onboarding stays gentle) + all 4 dungeon bosses; surfaced in all three roll overlays, `BattleLogEntry`, `LastActionSummary`, and as planning chips on `MonsterCard`. **Always-visible enemy roll**: the ability + spell overlays now **animate** the monster's d10 counter (spin → settle) in the shared `MonsterCounterPanel` (was a tiny static die) — the player always sees the enemy roll on a landed counter. **Combat-audit fixes** (from the prior `/code-audit`): (1) `SpellRollOverlay` now gets `outcome` + `playerDefFailed`, so a spell kill shows "🏆 Monster slain!" instead of a dead-monster counter, and a DEF-fail reads "💥" instead of "🛡️ held"; (2) **Storm Djinn → `attackType: 'magic'`** (lightning elemental; affinity was inverted) with **ATK retuned 40 → 34** since armor-ignoring magic is far deadlier vs high-DEF — the balance model's L20 band now exercises the magic path (Warrior 88% / Wizard 40% / Rogue 73% HP-loss); (3) ability **fizzle** damage floors at `MIN_DAMAGE` + rolls monster DEF-fail like every other path (no more hard-0 fizzles); (4) `MonsterCounterPanel` shows the numeric DEF value for parity with `ActionRollOverlay`; (5) character sheet surfaces a **"Fragile Armor"** callout for sub-1 DEF-multiplier classes (the Wizard's otherwise-invisible physical fragility); (6) **Dragon King kept physical** (baseline magic would nullify its signature 30%-HP ignore-DEF enrage) and given a `burst` special instead. **Player-stun deferred** (needs new turn-skip state across the hook + action bar + 3 pages — flagged as a fast-follow). Static catalog only → **no Firestore/rules/CF parity change** (combat damage is client-side); doc-count validator hardened to anchor the monster regex to the 4-space top-level indent so nested `specialMoves` ids don't inflate the count. 21 new vitest specs (`monsterSpecials.test.ts`); 1015 → 1036 root, functions 44 still green, build clean.

**Combat overlay consistency + meditate/attack crit fixes (2026-05-31)** — A consistency + accuracy pass making combat look and behave identically across all three surfaces (arena, dungeon run, wanted hunt). **UI**: new `CombatOverlays` dispatch component replaces the copy-pasted action/ability/spell overlay block in all three pages (single-touch overlay changes, no drift). The **ability overlay (`DiceRollOverlay`) finally shows the monster counter-attack** it always lacked — extracted a shared `MonsterCounterPanel` (also used by `SpellRollOverlay`) rendering the enemy strike with one vocabulary: 😵 stun · 💨 dodge · 🔮/⚔️ damage-type (magic shows "ignores armor", no DEF line) · 💥 DEF-fail · 🏆 slain on a kill. Shared `CritFlourish` (✦ Spirit Crit) surfaces crits on attacks/abilities/spells alike. `ActionRollOverlay` + the failed-escape branch now render magic counters correctly (🔮 ignores armor instead of the physical `− 🛡️ DEF` math). To feed the ability overlay, `resolveAbility` exposes `monsterRoll` and the ability/spell pending payloads now carry the full resolved counter (`monsterRoll`/`monsterStunned`/`monsterDamage`/`dodged`/`monsterAttackType`). **Logic**: (1) **meditate uses `effectiveStat(character, 'wisdom')`** — the only combat formula still on the raw stat; a Wizard's WIS multiplier now boosts magic restore. (2) **basic attacks can Spirit-crit** — `resolveAttackAction` rolls `rollSpellCrit` like abilities/spells (client-only, no server parity); the balance-model offense folds in crit EV to stay an honest mirror. (3) `CombatActionBar` Attack/Magic/Run sublabels corrected to use `effectiveStat`, matching live damage/escape math. 4 new vitest specs (1011 → 1015); functions 44 still green; production build clean.

**Combat & class balance pass + physical/magic damage types (2026-05-31)** — Follow-up tuning to the effective-stat layer below (its multiplier values were never play-tested live). A `/balance-check` found three gaps: players outscaled un-retuned monsters, the Rogue had no real weakness (the old `HP = 50 + STA×2 + HEALTH×1` let its Stamina multiplier double as tankiness), and flat-subtraction DEF made high-DEF builds floor monster damage. Fixes: **(1) HP formula flipped** to `50 + STA×1 + HEALTH×2` — Health is now the max-HP stat, Stamina drives the ability pool (mirrored in `functions/` pool copy). **(2) Multiplier matrix compressed** (1.5→1.35–1.4, 0.7→0.8) and the Rogue rebuilt around dodge + abilities with low Health as its weakness. **(3) Physical/magic damage types** — new `MonsterDef.attackType` (default `'physical'`; casters/elementals + the Necromancer boss tagged `'magic'`), new `incomingMonsterDamage` helper centralizing every monster→player hit (physical reduced by effective DEF; **magic ignores armor**, scaled by a new per-class `CLASS_DAMAGE_TAKEN` table — Warrior ×1.3, Wizard ×0.75, Rogue ×1.0). Surfaced as a 🔮/⚔️ tag on `MonsterCard` + combat log and a Weak-to-Magic / Magic-Ward callout on the character sheet. **(4) Monsters retuned** to the new baseline (mid/late ATK + HP bumps); XP/economy curves unchanged. New `balanceModel.test.ts` (pools + kill-rounds + damage-taken per class at L1/L10/L20) doubles as calibration tool + regression guard; Rogue dodge now also surfaces in `SpellRollOverlay`. 1005 root tests + 44 functions tests pass.

**Class stat multipliers made real + combat affinities + Rogue dodge (2026-05-31)** — The per-class `statMultipliers` shown on the character sheet (Warrior STR ×1.5 / DEF ×1.5, Wizard WIS ×1.5 / DEF ×0.7, Rogue AGI/STA ×1.5, …) were pure UI decoration — no combat, leveling, or pool code consumed them, so every class scaled identically (differentiated only by starting stats, the Wizard +10 magic pool, and subclass passives). Now made **real** via a single **effective-stat layer**: new `effectiveStat(character, statKey)` in `combat.ts` (`floor(base × statMultipliers[statKey])`, gear added flat) threaded through every combat + resource-pool formula — outgoing damage (`calculateRound`, `resolveAbility`, `resolveSpell` use effective STR/WIS), **incoming "weak to physical"** affinity (`effectivePlayerDefenseVsMonster` uses effective DEF — Wizard ×0.7 fragile, Warrior ×1.5 tanky; abilities/spell counters now route through this shared helper), escape (`rollRunAway` effective AGI), spell/ability crit (effective Spirit), and `playerMaxHp`/`playerMaxStamina`/`playerMaxMagic` (effective STA/HEALTH/WIS). **Rogue dodge** — new `classDodgeChance`/`rollClassDodge`: Rogue-only Agility-scaled chance (`ROGUE_DODGE_PER_AGILITY` 0.5%/pt, `ROGUE_DODGE_CAP` 25%) to fully negate a monster hit; applied at every incoming-hit chokepoint (`resolveRoundOutcome` via new `dodgeRng`, recovery free hit, failed-escape hit), surfaced as a new `dodged` `RoundEntry`/`PendingAction` field with a "💨 Dodged!" line in `BattleLogEntry`, `LastActionSummary`, and `ActionRollOverlay`. **Character sheet rewrite**: the misleading ×grid became an honest **Class Traits** panel (each multiplier + what it drives) plus class-only perks (Wizard Arcane Reserves +10 magic, Rogue Evasion live %, subclass teaser); `CharacterCard` shows each primary stat's effective in-combat value. **CF pool parity**: `functions/src/gameLogic/combat.ts` mirrors the multipliers via `CLASS_POOL_MULTIPLIERS` + `effectiveStat`; `playerMaxHp`/`playerMaxStamina` gained a `charClass` param (3 callers updated); the client↔functions parity test now cross-checks pools per class. Combat damage is client-only so affinities/dodge need no server parity. 12 new vitest specs (`classModifiers.test.ts`) + updated combat/spell/parity/dungeon fixtures (969 → 981 root; functions 44). **Balance caveat:** multiplier values were never live — a `/balance-check` + game-designer tuning pass is the recommended follow-up (Rogue STA ×1.5 inflates HP + ability pool; Wizard WIS ×1.5 now boosts magic damage **and** pool).

**Active focus:** Health-data integration scaffold landed (feature-flagged off). To enable, set the **Strava** secrets — the works-today free path, no approval needed — and flip `NEXT_PUBLIC_HEALTH_SYNC_ENABLED` (runbook in [docs/HEALTH-INTEGRATION.md](docs/HEALTH-INTEGRATION.md) §7). Garmin-direct (steps + sleep) waits on enterprise Developer Program approval; a paid aggregator like Terra/Rook remains a one-adapter upgrade on the provider-neutral core. Next on the roadmap: with the Wanted Board PR1 (currency) + PR2 (Hunt fork) now shipped, remaining Reputation polish is a hardening `claimBounty` Cloud Function (make the Reputation grant server-authoritative before leaderboards) + rank stat bonuses; then **Champions** (the next big feature, gated on Reputation). Spec: `docs/superpowers/specs/2026-05-17-future-features-roadmap-design.md`. The Achievements catalog view (locked/unlocked states, descriptions, gold reward display) already shipped as the default tab of `/collections`.

**Next priorities (post-MVP backlog, prioritized):**

### Balance & engine fixes (from game-systems-audit spec — fix before adding features)

- ~~**P0-1** — Unify monster counter-attack formula~~ — verified already flat (`monster.attack − effectivePlayerDef`); no change needed
- ~~**P0-2** — Monster XP scaling cliff at level 10~~ — shipped: `monsterXpScaling(playerLevel, monsterLevel)` adds +8%/level over the monster for top-tier (level ≥ 8) monsters, capped at 2.0×
- ~~**P0-3** — Daily combat XP cap / diminishing returns~~ — shipped: new `claimCombatVictory` Cloud Function applies a tiered multiplier (1.0× / 0.5× / 0.25× / 0.1×) at win counts 10 / 20 / 30. Gold never diminished. Combat page shows live "Daily combat XP" badge (emerald → amber → rose) with wins-until-next-penalty copy. Toast surfaces the multiplier on every penalized claim. `combatXpDailyMultiplier` lives in `src/lib/gameLogic/combat.ts` with parity copy in `functions/src/gameLogic/combat.ts` (parity test cross-checks the two for 0–100 wins). **Closes the post-MVP balance backlog — every P0/P1/P2 item is now shipped or verified-as-already-complete.**
- ~~**P1-1** — Steepen quest XP scaler~~ — shipped: `0.4 + 0.6·√l` (was `0.6 + 0.4·√l`); level-10 lifts 1.86× → 2.30×
- ~~**P1-2** — Raise Blessed tier streak XP multiplier 1.25× → 1.50×~~ — shipped
- ~~**P1-3** — Gold endgame sinks~~ — shipped: 100g quest reroll mechanic on every active QuestCard (excludes currently-held questDefIds for genuine variety); dungeon entry fees verified already deducted
- ~~**P1-4** — Fizzle stamina refund~~ — shipped: `COMBAT.FIZZLE_STAMINA_REFUND = 5` returned on failed ability roll
- ~~**P1-5** — Add level-9 monster (Lich King)~~ — shipped: HP 150 / atk 28 / def 9 / xp 220 / gold 110 with necromancer loot table
- ~~**P1-6** — Dungeon resource persistence~~ — verified already complete: `DungeonRun` doc carries `currentHp` / `currentStamina` / `currentMagic`; `dungeonStore.advanceRoom()` persists them; `bootstrap()` on the run page restores them; `enterRoom()` does not reset player resources. Audit framing was outdated.
- ~~**P2-1** — Mastery linked-stat hint on activity log form~~ — shipped: inline indigo callout shows the activity → stat mapping for workout / run / steps tabs
- ~~**P2-2** — Wizard starting stats~~ — shipped: starting health 6 → 8
- ~~**P2-3** — Activity cap proximity indicator on the log form~~ — shipped: subscribes to last-50 logs, computes today's total via UTC-day filter, shows a coloured cap meter (emerald → amber at 70% → rose when exhausted). Two helpers (`remainingCapacityForActivity`, `dailyCapUsageFraction`) with 7 new unit tests.
- ~~**P2-4** — Quest reroll mechanic~~ — shipped alongside P1-3 (100g per reroll, excludes held questDefIds). Quest pool expansion deferred — current pool (28 daily / 14 weekly) already supports variety.

### Feature backlog (dependency order — each unlocks the next)

- ~~**Achievements catalog view**~~ — shipped as the default tab of `/collections` (locked/unlocked states, per-achievement descriptions, gold reward display for all 31 achievements). Original proposal was `/character/achievements`; collections hub is the better home.

1. **Reputation / Wanted Board** — _PR1 (2026-05-31): dual-track currency, 5-tier rank, `/wanted` daily bounties, Loot path. PR2 (2026-05-31): the **Hunt fork** — combat bounties (train→fight→collect) via the wired `claimBounty` `path: 'fight'` seam + `/wanted/hunt/[bountyId]` route._ Remaining: a hardening `claimBounty` CF (server-authoritative Reputation) and rank stat bonuses. Spec: `2026-05-17-future-features-roadmap-design.md`
2. **Champions** — 10-champion roster, dungeon deployment, injury/recovery system, 7 archetypes (Warrior/Mage/Rogue/Ranger/Paladin/Necromancer/Bard), pip-dot cooldown UI; spec: `2026-05-17-champions-reputation-streaks-design.md`; requires Reputation
3. **Guilds** — level-15 unlock, activity-aligned XP drip, rank milestones, exclusive gear; requires Reputation
4. **Pets** — milestone/birthday unlock, 3 active slots, passive+active abilities scaled by rarity; requires Reputation/Champions groundwork
5. **Monthly NPCs** — 6 rotating NPCs with seeded challenge pools, permanent expiry after each month, level/reputation gates; requires Champions + Reputation
6. **Raids** — bi-weekly events, 5-day streak gate, 2+ champions required, god-tier loot, streak makeup mechanics; requires Champions + Guilds
7. **Territory / Map** — GPS-based zone claiming, PvP disputes with 3-day response window; long-horizon feature, requires all above
8. **Prestige / Ascension** — reset for permanent bonuses; design TBD (no spec yet)
9. **PWA** — installable mobile experience (no spec yet; can land independently)
10. **Health-data integration** — _scaffold shipped (Strava + Garmin, free, 2026-05-30; see above)._ To enable **Strava** (works today): create a Strava API app, set `STRAVA_CLIENT_ID`/`STRAVA_CLIENT_SECRET`/`STRAVA_VERIFY_TOKEN` + `STRAVA_REDIRECT_URI`, create the push subscription, flip `NEXT_PUBLIC_HEALTH_SYNC_ENABLED=true` (runbook §7). **Garmin** (steps + sleep) waits on enterprise Developer Program approval, then its secrets + post-approval constants (§8). **Apple Health** needs a native iOS/Capacitor shell. A paid aggregator (Terra/Rook) remains a one-adapter upgrade on the provider-neutral core
11. **Leaderboards** — compare with other users (no spec yet; requires user growth)

**Update protocol:** when a feature ships, move it from "Next" to "Shipped", bump the date, and append an entry to [docs/CHANGELOG.md](docs/CHANGELOG.md). This section in CLAUDE.md is the **canonical** status snapshot — `memory/project_state.md` no longer tracks status, only deep implementation details.

---

## How to Work

**Commands** (run from repo root):

- `npm run dev` — start dev server on `http://localhost:3000`
- `npm run typecheck` — `tsc --noEmit`. Fast TS-only check
- `npm run lint` — ESLint check
- `npm test` — `vitest run` (pure game-logic unit tests in `src/lib/gameLogic/__tests__/`)
- `npm run test:watch` — vitest in watch mode while developing
- `npm run test:coverage` — coverage report via `@vitest/coverage-v8`
- `npm run build` — production build. Catches everything above + build-time issues
- `npm run start` — serve the built output
- `npm run validate:docs` — verify the docs match source: headline counts (items, spells, monsters, achievements, quest pools, silhouettes), per-item silhouette coverage, and GAME-LOGIC.md export coverage. Zero-dependency; runs in CI. Fails with the computed values when a doc has drifted — update the doc prose, not the script
- `npm run validate:indexes` — validate `firestore.indexes.json` schema + required-index coverage
- `npm run test:rules` — Firestore security-rules tests (requires Firebase emulator). Run via: `npx firebase emulators:exec --only firestore --project demo-fitness-rpg "npm run test:rules"`. **Java 11+ must be on `PATH`** — the emulator is a JVM process. Not needed for any other dev command.

**Runtime:** CI runs on Node 24. Node 20/22 still work locally but are not tested in CI — prefer Node 24 to match.

**Verification:**

- Unit tests cover pure game logic (combat, spells, xp). UI / behavior changes still must be verified manually in the browser — golden path _and_ edge cases. The Playwright suite (`tests/e2e/`) covers auth redirects, login/register form attributes, and dark-mode input backgrounds; it does **not** cover authenticated game screens (no Firebase emulator), so those still require manual verification
- New game-logic functions in `src/lib/gameLogic/` should ship with vitest tests when feasible
- Before opening a PR: hooks (`typecheck` + `lint` + `vitest run`) pass + manual browser verification of any UI/UX change
- CI runs the same checks via `.github/workflows/` on every push

**Firebase:** live project `fitness-rpg-claude`. No emulator setup; dev hits live Firestore. Be intentional about test data — it's real.

**Cloud Functions runtime:** `functions/package.json` pins `"node": "22"` — Firebase Cloud Functions does not yet support Node 24 in production (Node 22 is the current max). This intentionally lags behind the CI runtime (Node 24). Do not bump the functions engine version without first confirming Firebase support.

**EBADENGINE warning:** running `npm install` inside `functions/` on Node 24 prints `npm warn EBADENGINE … required: { node: '22' }`. This is expected — the engine pin is for the Firebase production runtime, not the local development environment. The warning is harmless and can be ignored.

**Changelog:** when a meaningful change ships on `master`, append an entry to [docs/CHANGELOG.md](docs/CHANGELOG.md). Skip trivial (typos, comment-only). Keep newest-first. **The `CHANGELOG.md` edit must be committed inside the same feature PR** — never as a separate follow-up commit or PR after the feature has already merged, because the pre-push hook blocks direct pushes to `master`, forcing an unnecessary second PR.

---

## Development Partner Role

You are a **senior full-stack engineer and game systems designer** co-creating this product. Your job is not to blindly execute — it is to co-create, challenge, and elevate.

### Core Responsibilities

- Always aim for the best long-term solution, not the quickest fix
- Actively challenge assumptions, logic, and design decisions when something could be improved
- Think in terms of scalability, maintainability, and extensibility
- Balance engineering quality with engaging game design

---

## Code Quality Standards (MANDATORY)

- Produce clean, production-ready code — no placeholders or pseudo-code unless explicitly requested
- Follow consistent patterns across the entire codebase (naming, structure, hooks, state management)
- Prefer modular, reusable components and utilities
- Avoid duplication — abstract shared logic when appropriate
- No over-commenting — add a comment only when the WHY is non-obvious
- All code must be complete and directly usable
- When modifying a file, return the FULL updated file

### Naming Conventions

- Components: `PascalCase`
- Hooks: `useXxx`
- Utilities: `camelCase`
- Types/interfaces: `PascalCase`, prefixed with `I` for interfaces where ambiguous
- Zustand stores: `useXxxStore`
- Firebase collections: `camelCase` plural (e.g., `characters`, `activities`)

---

## UI / UX & Design Consistency

- Maintain the fitness + fantasy RPG aesthetic throughout
- Design should feel like a **game**, not a dashboard
- Use clear hierarchy, spacing, and alignment
- Required game UI patterns:
  - Progression feedback: XP bars, level indicators, stat numbers
  - Visual rewards: rarity color system, icons, micro-animations
  - Clear affordances: button states, loading states, feedback toasts
- Rarity color scale: Common (gray) → Uncommon (green) → Rare (blue) → Epic (purple) → Legendary (orange/gold)

---

## Game Design Principles

Think like a game designer, not just a developer. When adding any feature, evaluate:

1. **Player motivation** — why does this feel rewarding?
2. **Progression loops** — short-term (daily) vs long-term (seasonal) goals
3. **Replayability** — does it stay engaging after week 1?
4. **Behavioral alignment** — does it reinforce real fitness habits?

Core game systems to keep internally consistent:

- XP & leveling curve
- Quest difficulty and reward balance
- Gear stat scaling with player level
- Combat encounter scaling

---

## Architecture & Scalability

- Separation of concerns: UI layer → hook layer → store layer → Firebase layer
- Data models must support future features (multiplayer, leaderboards, guilds, seasons)
- Firebase reads/writes go through utility functions in `src/lib/`, never directly from components
- Zustand stores are the single source of truth for game state; Firebase is persistence
- Avoid client-side secrets — all sensitive logic goes in Firebase Security Rules or server actions

---

## Collaboration Style

- **Do NOT blindly agree** — push back when needed
- If an idea is flawed, explain why and offer a better alternative
- When appropriate, offer 2–3 approaches with tradeoffs
- Ask clarifying questions when it improves the outcome

---

## Git Workflow

### Before every PR — documentation checklist

**Run this before staging any commit. Do not open a PR until every applicable item is done.**

| Document                            | Update when…                                                                                |
| ----------------------------------- | ------------------------------------------------------------------------------------------- |
| `CLAUDE.md` — Shipped status + date | A feature, fix, or audit lands on `master`                                                  |
| `docs/CHANGELOG.md`                 | Any meaningful change — **bundle in the same PR, never a follow-up**                        |
| `docs/ARCHITECTURE.md`              | New `src/lib/` file, store, route, hook, or folder; new component pattern                   |
| `docs/UI-UX-MODERNIZATION.md`       | UI component added or changed; dark-mode coverage changed; new design pattern established   |
| `docs/GAME-LOGIC.md`                | New or changed exported function in `src/lib/gameLogic/`; catalog count changed             |
| `docs/FIRESTORE.md`                 | Schema field added/removed, collection changed, index added/removed                         |
| `docs/ART-ASSETS.md`                | Silhouette/icon added or changed; per-item art coverage changed                             |
| `docs/CI.md`                        | CI workflow file changed; new test suite added; husky hooks changed                         |
| `docs/DEPLOYMENT.md`                | New Cloud Function, secret, env var, or deploy-ordering change                              |
| `docs/SMOKE-TEST.md`                | New manual verification steps are needed for a feature                                      |
| Spec / design docs in `docs/`       | A spec item was implemented — mark it done and note any deviations from the original design |

**Quick rule:** if you touched anything in `src/lib/`, `src/components/`, `src/store/`, `src/hooks/`, `tests/`, `functions/`, or `.github/` — at least one doc probably needs updating. When in doubt, update it. A doc PR is cheap; stale docs compound silently.

**Count guard:** headline counts (items, spells, monsters, achievements, item silhouettes) are enforced by `npm run validate:docs` in CI — if you add content to `items.ts` / `monsters.ts` / `achievements.ts`, the build fails until the prose in README.md / GAME-LOGIC.md / ART-ASSETS.md is updated to match. Run it locally before opening the PR.

---

### Commit messages (imperative prose)

- Subject ≤ 50 characters, imperative mood, no trailing period
  - Good: `Add streak loot multiplier`, `Fix awardMastery stat overflow`
  - Bad: `Added streak system.`, `Updates and fixes`
- Blank line + body for any commit beyond a one-line trivial change
- Body explains _why_, not _what_ (the diff shows what)
- Always include `Co-Authored-By: Claude <model> <noreply@anthropic.com>` when Claude wrote or edited code

### Branching

- Claude worktree branches use auto-generated names — fine, treat them as disposable
- Human-initiated branches: `feat/<topic>`, `fix/<topic>`, `refactor/<topic>`, `chore/<topic>`
- Always branch from latest `master`

### Merging

- **Squash-merge via GitHub PR.** One clean commit per PR on master
- The PR title becomes the squash commit subject — PR titles must follow the commit-message rules above
- After a PR merges: delete the local branch and remove the worktree (`git worktree remove <path>`). Don't let merged branches accumulate

### Git hooks (husky + lint-staged)

- **pre-commit:** runs `lint-staged` (ESLint on staged `.ts`/`.tsx`) + `npm run typecheck` (project-wide) + `npm test` (vitest unit tests). Blocks commits that fail type, lint, or tests
- **pre-push:** blocks direct pushes to `master` (use PRs). Bypass in a true emergency only: `HUSKY=0 git push ...`
- Hooks activate via the `prepare` script — they install on every `npm install`. If a fresh clone's hooks aren't firing, re-run `npm install`

### DO NOT

- Never commit `.env.local` or any file matching `.env*.local`
- Never commit `.claude/settings.local.json` (gitignored — keep it that way)
- Never call Firestore directly from React components — go through `src/lib/` utilities or Zustand store actions
- Never bypass Firestore security rules client-side
- Never use `--no-verify`, `git push --force` to a shared branch, or `git reset --hard` over uncommitted work

---

## Output Format

Every substantive response should end with two sections:

### Next-Level Suggestions

Ideas to improve the feature or system further.

### Potential Risks / Gaps

Things that might break, scale poorly, or reduce player engagement.

---

## Available Sub-Agents (via `/agent`)

| Agent               | Purpose                                                  |
| ------------------- | -------------------------------------------------------- |
| `game-designer`     | Balance, mechanics, progression, player psychology       |
| `systems-architect` | Data models, Firebase schema, scalability review         |
| `ui-critic`         | Visual consistency, game feel, UX patterns               |
| `code-reviewer`     | Pattern consistency, anti-patterns, refactor suggestions |

## MCP / Tool Usage

Pick the right tool the first time — don't fall back to general-purpose tools when a connector is wired.

| Task                                                | Use this                                       | Don't use                               |
| --------------------------------------------------- | ---------------------------------------------- | --------------------------------------- |
| Library docs (Next.js, Firebase, Zustand, Recharts) | `context7` plugin (`query-docs`)               | WebFetch, training-data recall          |
| Querying live Firestore data / Auth users           | Firebase MCP                                   | `firebase` CLI shell-out                |
| Reading deployed Firestore security rules           | Firebase MCP                                   | `firebase firestore:rules:get`          |
| Ad-hoc UI verification after a code change          | `Claude Preview` (`preview_*`)                 | asking user to "test it in the browser" |
| Authoring E2E / regression tests in `tests/e2e/`    | `playwright` plugin (`browser_*`)              | Claude Preview                          |
| GitHub PR / issue / CI ops                          | `github` plugin / `gh` CLI                     | manual git commands for PR work         |
| Searching past Claude sessions for context          | `ccd_session_mgmt__search_session_transcripts` | rereading old chats by hand             |

**Firestore safety:** Firebase MCP can hit the live `fitness-rpg-claude` project. Default to read-only queries. Confirm before any write/delete against production data — CLAUDE.md's "be intentional about test data" rule applies double here.

## Available Slash Commands

| Command          | Purpose                                  |
| ---------------- | ---------------------------------------- |
| `/game-review`   | Full game design audit of a feature      |
| `/code-audit`    | Code quality + pattern consistency check |
| `/balance-check` | XP/reward/progression balance analysis   |
| `/schema-review` | Firestore data model review              |
